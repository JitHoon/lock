extends ../base
include ../mixins/lockers

block content
    div= Object.values(lockers[0].lockerNum)
    
    each i in [1,2,3,4,5,6,7,8,9]
        div= Object.values(lockers[i].lockerNum).join("")

    a(href="/admin/adlocker").homeCard__shape
            span.homeCard__join  사물함 데이터 등록
    
    div.arrow-switch
        a(href="/").arrow-switch 뒤로가기 &larr;