extends ../base

block content
    if _id === process.env.ADMIN
        form(method="POST" action="/admin/"+_id+"/qna/"+question._id+"/editq") 
            input(placeholder="질문 내용", required, type="text", name="content", value=question.content)
            input(value="질문 수정하기",type="submit")
    else
        form(method="POST" action="/qna/"+question._id+"/editq") 
            input(placeholder="질문 내용", required, type="text", name="content", value=question.content)
            input(value="질문 수정하기",type="submit")