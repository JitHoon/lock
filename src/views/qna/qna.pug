extends ../base
include ../mixins/questions

block content
    div.arrow-switch
        a(href="qna/uploadq") 질문하기 &rarr;
                
    each question in questions
        +questions(question)
    else
        li Sorry nothing found.
    h1 
