if loggedIn
    nav.Menu
        ul
            li
                a(href="/users/logout").Menu__logout
                    i.logout__btn 로그아웃
                if _id === process.env.ADMIN
                    a(href="/admin/" + process.env.ADMIN).Menu__home
                        i.fa-solid.fa-house
                else
                   a(href="/").Menu__home
                        i.fa-solid.fa-house
                a(onclick="history.back()").Menu__back
                    i.fas.fa-arrow-left.fa-2x
else
    nav.Menu
        ul
            li
                a(href="/").Menu__home2
                        i.fa-solid.fa-house