extends ../base

block content
    if errorMessage
            span=errorMessage

    form(method="post")
        input(name="oldPW", placeholder="Old Password", type="password")
        input(name="newPW1", placeholder="New Password", type="password")
        input(name="newPW2", placeholder="New Password Confirmation", type="password")
        input(value="Change Password", type="submit")