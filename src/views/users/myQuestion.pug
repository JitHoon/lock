extends ../base
include ../mixins/questions

block content
    form(method="POST")
        div.center
            div.profile__data
                label(for="") 이름
                p=loggedInUser.userName
            div.profile__data
                label(for="") 학번
                p=loggedInUser.studentID
            div.profile__data
                label(for="") 폰번호
                p="0"+loggedInUser.phoneNumber

        div.center    
            div.arrow-switch__myProfile
                a(href=`/users/${loggedInUser._id}/edit`) 내 사물함 &rarr;

            div.arrow-switch__myProfile
                a(href=`/users/${loggedInUser._id}/q`) 내 질문 &rarr;

            div.arrow-switch__myProfile
                a(href=`/users/${loggedInUser._id}/edit`) 폰 번호 변경 &rarr;

            div.arrow-switch__myProfile
                a(href=`/users/${loggedInUser._id}/changePassword`) 비번 변경 &rarr;

    div.center    
        div.arrow-switch
            a(href=`/users/${loggedInUser._id}`) 뒤로가기 &larr;

    div.question-grid
        each question in user.questions
            +questions(question)
        else
            span.empty__message 내 질문이 없습니다.🥲